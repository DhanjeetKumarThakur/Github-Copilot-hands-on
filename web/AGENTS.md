# Web/ -- React Front-End Instructions

- Use function Components and React hooks only; no class components.
- Co-locate component styles with the component.
- Use the existing design tokens in `web/src/theme.ts`; do not hardcode colors. 
- Every component gets a Vitest + React Testing Library test. cre